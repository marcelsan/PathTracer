
#include <glm/glm.hpp>

#include "scene.h"
#include "color.h"

using namespace glm;

int main(int argc, char const *argv[])
{
	vec3 x(1, 0, 0);
	vec3 y(0, 1, 0);
	vec3 z(0, 0, 1);

	PathTrace::color r(1, 0, 0);
	PathTrace::color g(0, 1, 0);
	PathTrace::color b(0, 0, 1);

	PathTrace::Scene s;

	return 0;
}